{% extends 'base.html.twig' %}

{% block body %}

    <div>
        <a href="{{ path('teacher') }}"> << Back </a>
    </div>

    <h2> create exams </h2>

    <div>
        {{ form_start(addExamForm) }}
        {{ form_widget(addExamForm) }}
        {{ form_end(addExamForm) }}
    </div>

    <div>
        <table>
            <thead>
            <tr>
                <th>Field</th>
                <th>Description</th>
                <th> Details </th>
                <!-- <th> Random questions </th>  -->
            </tr>
            </thead>
            <tbody>
            {% for exam in exams %}
                <tr>
                    <td>{{ exam.field.name  }} </td>
                    <td>{{ exam.name }} </td>
                    <td> <a href="{{ path('examQuestion', {'eId': exam.id}) }}">Edit exam</a> </td>
                  <!--  <td>
                        Select random questions =>
                        <form action="{{ path('randomSelection', {'eId': exam.id}) }}" method="post">
                            number
                            <input type="number" name="randomNumber" id="number">
                            <button type="submit">Generate</button>
                        </form>
                    </td>
                 -->
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <div>
        <a href="{{ path('logout') }}">Logout</a>
    </div>


{% endblock %}